App\Entity\Subscription:
  subscription_admin:
    user: '@user_admin'
    plan: 'premium'
    status: 'active'
    stripeSubscriptionId: 'sub_admin_premium_123'
    currentPeriodStart: <dateTimeBetween('-1 month', 'now')>
    currentPeriodEnd: <dateTimeBetween('+1 month', '+2 months')>
    createdAt: <dateTimeBetween('-6 months', '-3 months')>
    updatedAt: <dateTimeBetween('-1 week', 'now')>

  subscription_test:
    user: '@user_test'
    plan: 'premium'
    status: 'active'
    stripeSubscriptionId: 'sub_test_premium_456'
    currentPeriodStart: <dateTimeBetween('-15 days', 'now')>
    currentPeriodEnd: <dateTimeBetween('+15 days', '+1 month')>
    createdAt: <dateTimeBetween('-2 months', '-1 month')>
    updatedAt: <dateTimeBetween('-3 days', 'now')>

  subscription_recruiter:
    user: '@user_recruiter'
    plan: 'free'
    status: 'active'
    stripeSubscriptionId: null
    currentPeriodStart: <dateTimeBetween('-3 months', '-1 month')>
    currentPeriodEnd: null
    createdAt: <dateTimeBetween('-3 months', '-1 month')>
    updatedAt: <dateTimeBetween('-1 month', 'now')> 